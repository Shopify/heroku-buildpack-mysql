#!/usr/bin/env ruby

# sync output
$stdout.sync = true

$:.unshift File.expand_path("../../lib", __FILE__)
require "build_pack"

build_dir = ARGV[0]
cache_dir = ARGV[1]

BuildPack.run(build_dir, cache_dir)

